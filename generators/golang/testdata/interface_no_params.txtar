Test interface generation for methods without parameters.

Flags: server, client

-- input.json --
{
  "metaData": {"version": "3.17.0"},
  "requests": [
    {
      "method": "shutdown",
      "documentation": "A shutdown request.",
      "messageDirection": "clientToServer",
      "result": {"kind": "base", "name": "null"}
    },
    {
      "method": "textDocument/foldingRange",
      "documentation": "A request to provide folding ranges.",
      "messageDirection": "clientToServer",
      "params": {"kind": "reference", "name": "FoldingRangeParams"},
      "result": {"kind": "array", "element": {"kind": "reference", "name": "FoldingRange"}}
    }
  ],
  "notifications": [
    {
      "method": "exit",
      "documentation": "Exit notification.",
      "messageDirection": "clientToServer"
    },
    {
      "method": "initialized",
      "documentation": "The initialized notification.",
      "messageDirection": "clientToServer",
      "params": {"kind": "reference", "name": "InitializedParams"}
    }
  ],
  "structures": [
    {"name": "FoldingRangeParams", "properties": []},
    {"name": "FoldingRange", "properties": []},
    {"name": "InitializedParams", "properties": []}
  ],
  "enumerations": [],
  "typeAliases": []
}

-- want/protocol.go --
// Code generated by lspls. DO NOT EDIT.
package protocol

import (
	"context"
	"encoding/json"
)

type FoldingRange struct {
}

type FoldingRangeParams struct {
}

type InitializedParams struct {
}

// LSP method names.
const (
	MethodExit                     = "exit"
	MethodInitialized              = "initialized"
	MethodShutdown                 = "shutdown"
	MethodTextDocumentFoldingRange = "textDocument/foldingRange"
)

// Server defines the LSP server interface.
type Server interface {
	// Exit notification.
	Exit(context.Context) error
	// The initialized notification.
	Initialized(context.Context, *InitializedParams) error
	// A shutdown request.
	Shutdown(context.Context) (*any, error)
	// A request to provide folding ranges.
	TextDocumentFoldingRange(context.Context, *FoldingRangeParams) ([]FoldingRange, error)
}
